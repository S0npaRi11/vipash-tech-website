const header = ({ heading }) => {
    return (
        <div id="header">
            <div className="container">
                <h2> { heading } </h2>
            </div>
        </div>
    )
}

export default header
